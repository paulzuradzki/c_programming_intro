#include <stdio.h>

int main (void)
{
  int val1, val2, sum, mystery;
  val1 = 100;
  val2 = 300;
  sum = val1 + val2;
  printf ("%i plus %i is %i\n", val1, val2, sum);
  printf ("mystery memory # is: %i\n", mystery);
  return 0;
}
